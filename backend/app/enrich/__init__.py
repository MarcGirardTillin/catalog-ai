"""Leg A pipeline steps (copy, images, title, weights).

Implemented: title templating, weight mapping/conversion (pure logic).
Planned: copy.py (Claude/OpenAI) and images.py (Photoroom) — they compose the
external clients from `app.clients`.
"""
