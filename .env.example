# Used by the backend to generate links in emails to the frontend
FRONTEND_HOST=http://localhost:5173

# Environment: local, staging, production
ENVIRONMENT=local

PROJECT_NAME="Full Stack FastAPI Project"
STACK_NAME=full-stack-fastapi-project

# Backend
BACKEND_CORS_ORIGINS="http://localhost,http://localhost:5173,https://localhost,https://localhost:5173"

# Postgres
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_DB=app
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changethis

# Auth (app-local users + JWT session cookie)
# Generate a strong secret, e.g. `python -c "import secrets; print(secrets.token_urlsafe(48))"`
APP_SECRET=dev-insecure-secret-change-in-production-please
ACCESS_TOKEN_EXPIRE_MINUTES=11520
AUTH_COOKIE_SECURE=false
# First user, created by `python -m app.initial_data`
FIRST_SUPERUSER=
FIRST_SUPERUSER_PASSWORD=

# Xano (Tillin REST API — read path). Auth is email/password login -> authToken.
# Leave empty to disable the integration. XANO_DATA_SOURCE=test hits the seeded
# test datasource (sent as the X-Data-Source header).
XANO_BASE_URL=
XANO_LOGIN_EMAIL=
XANO_LOGIN_PASSWORD=
XANO_DATA_SOURCE=
XANO_TIMEOUT_SECONDS=30

# External services (empty key = integration disabled; never commit real keys)
ANTHROPIC_API_KEY=
AI_DEFAULT_MODEL=claude-sonnet-5
PHOTOROOM_API_KEY=
FIRECRAWL_API_KEY=
BREVO_API_KEY=
BREVO_SENDER_EMAIL=
BREVO_SENDER_NAME=CatalogAI

# Configure these with your own Docker registry images
DOCKER_IMAGE_BACKEND=backend
DOCKER_IMAGE_FRONTEND=frontend
